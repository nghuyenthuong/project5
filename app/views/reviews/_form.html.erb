<%= form_with(model: review) do |form| %>
<% if review.errors.any? %>
<div id="error_explanation" class="bg-red-50 p-6 rounded text-red-800">
  <h2 class="text-lg font-semibold mb-3"><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

  <ul class="list-disc leading-relaxed">
    <% review.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <div class="mb-6">
        <%= form.label :customer_id, class: label_class %>
          <%= form.number_field :customer_id, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :package_id, class: label_class %>
          <%= form.number_field :package_id, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :rating, class: label_class %>
          <%= form.number_field :rating, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :review_text, class: label_class %>
          <%= form.text_area :review_text, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :review_date, class: label_class %>
          <%= form.datetime_field :review_date, class: input_class %>
      </div>

<%= form.submit class: button_class(theme: 'primary') %>

<% if review.persisted? %>
<%= link_to "Cancel", review, class: "text-neutral-700 underline inline-block ml-3" %>
<% else %>
<%= link_to "Cancel", reviews_path, class:  "text-neutral-700 underline inline-block ml-3" %>
<% end %>
<% end %>
